export const $=(s,r=document)=>r.querySelector(s);export const $$=(s,r=document)=>[...r.querySelectorAll(s)];export const field=(label,name,value='',type='text')=>`<label><span>${label}</span><input name="${name}" type="${type}" value="${value??''}"></label>`;export const select=(label,name,opts,value)=>`<label><span>${label}</span><select name="${name}">${opts.map(o=>`<option value="${o.value}" ${String(o.value)===String(value)?'selected':''}>${o.label}</option>`).join('')}</select></label>`;export const card=(body,cls='')=>`<section class="card ${cls}">${body}</section>`;
